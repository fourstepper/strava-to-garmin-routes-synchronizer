<%= form_with(model: route) do |form| %>
  <% if route.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(route.errors.count, "error") %> prohibited this route from being saved:</h2>

      <ul>
        <% route.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :route_id, style: "display: block" %>
    <%= form.number_field :route_id %>
  </div>

  <div>
    <%= form.label :route_updated_at, style: "display: block" %>
    <%= form.datetime_field :route_updated_at %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
